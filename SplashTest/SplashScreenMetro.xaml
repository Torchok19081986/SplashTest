<Controls:MetroWindow
    x:Class="SplashTest.SplashScreenMetro"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SplashTest"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="SplashScreenMetro"
    Width="800"
    Height="148.422"
    Background="AliceBlue"
    ResizeMode="NoResize"
    UseNoneWindowStyle="True"
    WindowStyle="None"
    DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}"
    mc:Ignorable="d" ContentRendered="MetroWindow_ContentRendered">
    <Grid>
        <Label
            x:Name="lblmessage"
            Grid.Row="0"
            Margin="10,105,128,10"
            Content="{Binding Path=Message}"
            FontFamily="Tahoma"
            FontSize="18"
            FontStretch="Normal"
            FontStyle="Normal"
            FontWeight="Bold"
            Foreground="Orange" />

        <Controls:MetroProgressBar x:Name="progressloadingindecator" Minimum="0" Maximum="100" Foreground="Orange" IsIndeterminate="True"  />
    </Grid>
</Controls:MetroWindow>
